[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "careeros"
version = "0.1.0"
description = "An AI-powered career operating system."
readme = "README.md"
requires-python = ">=3.12"
license = { text = "Proprietary" }
authors = [
    { name = "careerOS Engineering" },
]
keywords = [
    "ai",
    "career",
    "resume",
    "job-search",
]
classifiers = [
    "Development Status :: 2 - Pre-Alpha",
    "Intended Audience :: End Users/Desktop",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
]
dependencies = [
    "SQLAlchemy>=2.0,<3.0",
    "pydantic>=2.7,<3.0",
    "psycopg[binary]>=3.2,<4.0",
    "python-docx>=1.1,<2.0",
    "reportlab>=4.2,<5.0",
    "email-validator>=2.2,<3.0",
    "playwright>=1.50,<2.0",
    "fastapi>=0.115,<1.0",
    "uvicorn>=0.34,<1.0",
    "python-dotenv>=1.0,<2.0",
    "openai>=2.41,<3.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0,<9.0",
    "pytest-cov>=5.0,<7.0",
    "ruff>=0.6,<1.0",
    "httpx>=0.28,<1.0",
]

[tool.setuptools.packages.find]
include = ["app*"]

[tool.pytest.ini_options]
addopts = "-ra"
testpaths = ["tests"]
markers = [
    "browser: optional Playwright browser tests; run explicitly after installing Chromium",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
